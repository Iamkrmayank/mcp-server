# Production Dependencies for Agno MCP Orchestration System
# Install with: pip install -r requirements.production.txt

# Core MCP dependencies
mcp>=0.9.0
httpx>=0.25.0
pydantic>=2.5.0
python-dotenv>=1.0.0
asyncio>=3.4.3
aiohttp>=3.9.0

# Production enhancements
redis>=4.5.0
asyncio-mqtt>=0.13.0
prometheus-client>=0.17.0

# Monitoring and observability
psutil>=5.9.0
structlog>=23.1.0

# Performance and caching
aioredis>=2.0.0
aiocache>=0.12.0

# Security
cryptography>=41.0.0
bcrypt>=4.0.0

# Testing and load testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
aioresponses>=0.7.4

# Development and debugging
ipython>=8.0.0
rich>=13.0.0

# Optional: HTTP server (if converting from MCP to HTTP)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# Optional: Database support
sqlalchemy>=2.0.0
alembic>=1.12.0

# Optional: Message queuing
celery>=5.3.0
kombu>=5.3.0

# Optional: Container orchestration
kubernetes>=28.1.0
docker>=6.1.0
